---
const appUrl = 'https://app.dwellhub.in';

const rows = [
  {
    label: 'Platform fee',
    icon: 'payments',
    others: '₹3–15 /flat/month base fee',
    perFlat: 5,
    ours: '₹20 /flat/month',
    oursNote: 'All core workflows included',
  },
  {
    label: 'Payment gateway',
    icon: 'account_balance',
    others: '1.5–2 % per txn (≈₹30 on ₹1,500 bill)',
    perFlat: 30,
    ours: '0 %',
    oursNote: 'UPI is free in India',
  },
  {
    label: 'Setup & onboarding',
    icon: 'handshake',
    others: '₹10,000+ one-time (≈₹17/flat/mo over 12 mo)',
    perFlat: 17,
    ours: 'Free',
    oursNote: 'Self-serve setup in minutes',
  },
  {
    label: 'Feature modules',
    icon: 'extension',
    others: '₹2–5 /flat extra per module (≈₹25 /flat total)',
    perFlat: 25,
    ours: 'Everything included',
    oursNote: 'Billing, complaints, gate management, reports, and more',
  },
  {
    label: 'SMS & notifications',
    icon: 'notifications',
    others: '₹500+ /month (≈₹10/flat for 50 flats)',
    perFlat: 10,
    ours: 'Free',
    oursNote: 'Push notifications & email',
  },
  {
    label: 'Contract lock-in',
    icon: 'lock',
    others: '12 months minimum',
    ours: 'None',
    oursNote: 'Cancel anytime',
  },
];

const totals = [
  { flats: 50, others: '~₹4,350', ours: '₹1,000' },
];

const summaryTotals = [
  {
    label: 'Per flat',
    others: '~₹87/month',
    ours: '₹20/month',
  },
  {
    label: '50-flat society',
    others: '~₹4,350/month',
    ours: '₹1,000/month',
  },
  {
    label: '100-flat society',
    others: '~₹8,700/month',
    ours: '₹2,000/month',
  },
];
---

<section id="pricing" class="pt-28 pb-16 px-6">
  <div class="max-w-5xl mx-auto">

    <!-- Header -->
    <div class="mb-12 text-center max-w-2xl mx-auto">
      <p class="text-xs font-bold tracking-widest uppercase text-[#4f46e5] mb-3 font-label">Simple Pricing</p>
      <h2 class="text-4xl font-extrabold text-primary tracking-tight mb-3 font-headline leading-tight">
        Transparent pricing. No hidden fees.
      </h2>
      <p class="text-on-surface-variant font-body text-sm">One price per flat, all features included. See how Dwell Hub compares.</p>
    </div>

    <!-- Pricing highlight cards -->
    <div class="mb-12 grid grid-cols-1 sm:grid-cols-3 gap-4 max-w-3xl mx-auto">
      <div class="rounded-2xl bg-white border border-slate-100 p-6 text-center">
        <div class="w-10 h-10 bg-emerald-50 rounded-xl flex items-center justify-center mx-auto mb-3">
          <span class="material-symbols-outlined text-emerald-600 text-xl">hourglass_top</span>
        </div>
        <p class="text-lg font-extrabold text-primary font-headline">30 days free</p>
        <p class="mt-1 text-xs text-on-surface-variant font-body">Full access, no card needed</p>
      </div>
      <div class="rounded-2xl bg-[#4f46e5] p-6 text-center shadow-lg shadow-indigo-500/15">
        <div class="w-10 h-10 bg-white/15 rounded-xl flex items-center justify-center mx-auto mb-3">
          <span class="material-symbols-outlined text-white text-xl">payments</span>
        </div>
        <p class="text-lg font-extrabold text-white font-headline">₹20 / flat / mo</p>
        <p class="mt-1 text-xs text-indigo-200 font-body">Everything included</p>
      </div>
      <div class="rounded-2xl bg-white border border-slate-100 p-6 text-center">
        <div class="w-10 h-10 bg-slate-50 rounded-xl flex items-center justify-center mx-auto mb-3">
          <span class="material-symbols-outlined text-[#4f46e5] text-xl">lock_open</span>
        </div>
        <p class="text-lg font-extrabold text-primary font-headline">No lock-in</p>
        <p class="mt-1 text-xs text-on-surface-variant font-body">Cancel anytime</p>
      </div>
    </div>

    <!-- Comparison Table -->
    <div class="rounded-2xl border border-slate-200 overflow-hidden bg-white">

      <!-- Table Header -->
      <div class="grid grid-cols-[1fr_1fr_1fr] border-b border-slate-200 bg-slate-50">
        <div class="p-4 lg:p-5">
          <span class="text-xs font-bold tracking-widest uppercase text-on-surface-variant font-label">Cost Item</span>
        </div>
        <div class="p-4 lg:p-5 border-l border-slate-200">
          <span class="text-xs font-bold tracking-widest uppercase text-on-surface-variant font-label">Typical Apps</span>
        </div>
        <div class="p-4 lg:p-5 border-l border-slate-200 bg-indigo-50/50">
          <div class="flex items-center gap-2">
            <span class="text-xs font-bold tracking-widest uppercase text-[#4f46e5] font-label">Dwell Hub</span>
            <span class="text-[9px] font-bold tracking-widest uppercase bg-emerald-100 text-emerald-700 px-2 py-0.5 rounded-full">Save 4×</span>
          </div>
        </div>
      </div>

      <!-- Rows -->
      {rows.map((row, i) => (
        <div class:list={["grid grid-cols-[1fr_1fr_1fr]", i < rows.length - 1 && "border-b border-slate-100"]}>
          <div class="p-4 lg:p-5 flex items-center gap-3">
            <div class="w-8 h-8 bg-slate-50 rounded-lg flex items-center justify-center flex-shrink-0 hidden sm:flex">
              <span class="material-symbols-outlined text-slate-500 text-lg">{row.icon}</span>
            </div>
            <span class="text-sm font-semibold text-primary font-headline">{row.label}</span>
          </div>
          <div class="p-4 lg:p-5 border-l border-slate-100 flex items-center">
            <span class="text-sm text-on-surface-variant font-body">{row.others}</span>
          </div>
          <div class="p-4 lg:p-5 border-l border-slate-100 bg-indigo-50/30 flex items-center gap-2">
            <span class="material-symbols-outlined text-emerald-500 text-base flex-shrink-0">check_circle</span>
            <div>
              <span class="text-sm font-bold text-primary font-headline">{row.ours}</span>
              <span class="block text-[11px] text-on-surface-variant font-body">{row.oursNote}</span>
            </div>
          </div>
        </div>
      ))}

      <!-- Totals -->
      <div class="border-t-2 border-slate-200">
        {summaryTotals.map((total, i) => (
          <div class:list={["grid grid-cols-[1fr_1fr_1fr]", i < summaryTotals.length - 1 && "border-b border-slate-100"]}>
            <div class="p-4 lg:p-5 flex items-center bg-slate-50/50">
              <span class="text-sm font-bold text-primary font-headline">{total.label}</span>
            </div>
            <div class="p-4 lg:p-5 border-l border-slate-100 flex items-center bg-red-50/40">
              <span class="text-base font-bold text-red-500 font-headline line-through decoration-red-300">{total.others}</span>
            </div>
            <div class="p-4 lg:p-5 border-l border-slate-100 flex items-center bg-[#4f46e5]">
              <span class="text-lg font-extrabold text-white font-headline">{total.ours}</span>
            </div>
          </div>
        ))}
      </div>
    </div>

    <!-- CTA -->
    <div class="mt-10 text-center">
      <a href={`${appUrl}/register`} class="inline-block bg-[#4f46e5] text-white px-10 py-4 rounded-full font-bold text-base tracking-wide hover:bg-[#4338ca] hover:shadow-lg hover:shadow-indigo-500/25 transition-all">
        Start your 30-day free trial
      </a>
      <p class="mt-3 text-xs text-on-surface-variant font-body">No credit card required · Cancel anytime</p>
    </div>

  </div>
</section>
